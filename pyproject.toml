# please keep this to a minimum - defaults are good
[tool.black]
include = '\.pyi?$'
exclude = '''
(
   /(   # exclude a few common directories in the root
      \.git
     | \.venv
     | venv
     | docs/_build
   )/
)
'''
